name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  First:
    runs-on: ubuntu-latest
    steps:
      - run: terraform init
        run: |
          ls ${{ github.workspace }}
